using System;
using System.Collections.Generic;
using UnityEngine;

namespace Petri.Formula.Test
{
    public class TestFormulaCreator : MonoBehaviour
    {
        public List<Reagent> Reagents;

        private void Start()
        {
            // if (Reagents == null)
            // {
            //     throw new Exception("Reagents list is null");
            // }
            //
            // var constructor = new FormulaConstructor().CreateFormula(3, 8);
            //
            // foreach (var simpleReagent in Reagents)
            // {
            //     constructor.AddReagent(simpleReagent, 0, 0);
            // }
        }
    }
}